# 🗺 OSM キャッシュの退避と pfaedle の無言スキップ

2026-09-01 に `Update OSM Data` が Geofabrik の HTTP 502 で失敗した．その調査で，Actions のキャッシュが 7 日で退避される性質とワークフローの条件分岐が噛み合っていた事実を掴んだ．経路形状の生成は長期にわたりスキップされていた．本書は経緯と対処，一般化できる教訓を残す．

## 📚 目次

- [🔎 発端](#-発端)
- [🕳 二段目の問題](#-二段目の問題)
- [🧩 原因の構造](#-原因の構造)
- [🛠 対処](#-対処)
- [💡 一般化できる教訓](#-一般化できる教訓)
- [🔁 後日談: すでに公開されていた欠落](#-後日談-すでに公開されていた欠落)
- [🩹 復旧とその副作用](#-復旧とその副作用)
- [🚧 残る課題](#-残る課題)

---

## 🔎 発端

月次の `update-osm.yml` が失敗した（[run 33479590413][run]）．Geofabrik が HTTP 502 を返し，`curl -f` が終了コード 22 で落ちた．

再試行の設定は `--retry 3 --retry-delay 5` であり，約 20 秒しか粘れていない．上流の一時障害であってリポジトリ側の欠陥ではない．調査時点で Geofabrik は復旧しており，同じ URL から 188 MB の取得に成功した．

ここで止めれば「再実行して終わり」だった．ただし失敗したワークフローが何のために存在するかを追ったところ，別の問題へ行き着いた．

---

## 🕳 二段目の問題

`update-osm.yml` の役目は OSM データを Actions のキャッシュへ置くことだけである．実際に使うのは週次の `update-gtfs.yml` であり，キャッシュを復元して pfaedle へ渡す．

しかし `gh cache list` に `osm-hokkaido-` で始まる項目が存在しなかった．Actions のキャッシュは 7 日間アクセスがないと退避される．月次で保存しても，週次のワークフローが必要とする時点では失われている．

さらに pfaedle のステップが次の条件を持っていた．

```yaml
if: steps.calendar.outputs.changed == 'true' && hashFiles('data/osm/hokkaido-latest.osm.pbf') != ''
```

キャッシュが無ければ条件が偽となり，ステップごとスキップされる．ジョブは緑のまま完了する．2026-08-24 の実行ログでも `Cache not found for input keys: osm-hokkaido-` を確認した．カレンダー変更を検知して変換まで走った回でも，経路形状は生成されていなかった．

---

## 🧩 原因の構造

表 1. 2 つの前提と，それらが噛み合って生んだ結果

| 前提 | 単体では妥当 | 組み合わせた結果 |
|---|---|---|
| キャッシュは 7 日でアクセスがなければ退避される | GitHub 側の仕様 | 月次保存では週次利用に間に合わない |
| ステップの条件に `hashFiles` を置く | 入力が無ければ動かさない自然な発想 | 失敗が緑のジョブとして埋もれる |

`scripts/run-pfaedle.sh` には，OSM ファイルが無ければ Geofabrik から取得するフォールバックが元から実装されていた．皮肉なことに，そこへ到達する前にワークフローの条件がステップごと消していた．防御を二重に用意し，外側が内側を無効化していた形である．

---

## 🛠 対処

対処は [PR #119][pr119] で行った．

表 2. 変更の内容と意図

| 変更 | 意図 |
|---|---|
| curl の再試行を `--retry 5 --retry-delay 20 --retry-all-errors --retry-max-time 600` へ拡張 | 数分規模の一時障害を吸収する |
| pfaedle ステップの `hashFiles` ガードを撤去 | フォールバックへ到達させ，失敗を赤いジョブとして表面化させる |
| キャッシュミス時に取得した pbf を保存し直すステップを追加 | 次回実行へつなぎ，取得の重複を避ける |
| フォールバック取得へ md5 検証を追加 | 破損した pbf で劣化した経路形状を作らない |
| 作業用の `/data/` を `.gitignore` へ追加 | 190 MB の pbf を誤って追跡しない |

`.gitignore` は `/data/` と先頭スラッシュ付きで書く．`data/` と書いた場合，追跡下の `public/data/` まで巻き込む．結果としてワークフローの `git add public/data/` が新規ファイルを拾えなくなる．

---

## 💡 一般化できる教訓

- **キャッシュの保存周期は，利用側の周期より短く保つ．** 保存側だけを見ると月次で足りるように見える．退避の期限を挟むと成立しない．
- **入力の有無でステップを分岐させない．** 分岐は失敗を成功に見せる．入力を用意する責任はステップの内側へ寄せ，足りなければ落とす．
- **緑のジョブは正常の証拠にならない．** 条件付きステップを持つワークフローでは，何が実行されたかまで見ないと劣化を検知できない．
- **失敗したジョブは，何のために存在するかまで遡る．** 今回は一過性の 502 が入口となり，半年近く気づかれなかった欠落へ届いた．

---

## 🔁 後日談: すでに公開されていた欠落

同日中に別の作業へ移る前，公開中のデータを実際に開いた．3 事業者すべて `shapes` が 0 件だった．地図へ経路が 1 本も描かれない状態が続いていたことになる．

前節までの対処は，これから起きるスキップを止めるものである．すでに公開された成果物には触れていない．実行履歴を遡ると，カレンダー変更を検知しながら pfaedle を飛ばした回が 2 度あった．

表 3. `asahikawa_denkikido.json` に含まれる経路形状の件数

| コミット | 日付 | pfaedle | `shapes` |
|---|---|---|---|
| `d752ff4` | 2026-08-10 | skipped | 0 |
| `dbdba35` | 2026-08-17 | success | 16894 |
| `f42bd2d` | 2026-08-24 | skipped | 0 |

復旧の手段も無かった．pfaedle はカレンダー期間の変更を条件とするため，手動で実行しても次のダイヤ改定までスキップされる．入口を塞いだだけでは，壊れた状態から戻れない．

表 4. 後続の対処

| 変更 | 意図 |
|---|---|
| `update-gtfs.yml` へ手動実行の `force` 入力を追加 | 期間が変わらなくても人が再生成できるようにする |
| `convert-gtfs.ts` へ `assertShapesPresent` を追加 | `shapes` が空の JSON を書き出さずジョブを落とす |

`scripts/validate-shapes.ts` は以前から存在した．ただし `run-pfaedle.sh` の内側でしか動かず，ステップごとスキップされた回には届かなかった．検査は生成の内側だけでなく，公開の直前にも要る．

ここから引ける教訓を 1 つ加える．**原因を止めることと，すでに出た被害を戻すことは別である．** 入口を塞いだ時点で終えず，塞ぐ前に何が通り抜けたかを数える．そして戻す手段を用意する．

---

## 🩹 復旧とその副作用

`force` を足しただけでは復旧できなかった．実行前に codex のレビューが 3 点を指摘し，いずれも復旧の手段そのものが持つ副作用だった．

表 5. レビューで見つかった副作用

| 指摘 | 何が起きるか | 対処 |
|---|---|---|
| 期間が同じ強制実行が前期間の JSON を消す | `convert-gtfs.ts` が保持中の `*_prev.json` を不要と判定する | 比較は常に走らせ，前期間の扱いは期間の変化で決める |
| 強制実行の結果がキャッシュへ残らない | 項目は不変であり，同じキーの保存は捨てられる | 保存キーへ実行 ID を足す |
| 前期間の JSON が検査を通らない | 現行だけ検査しても，前期間が欠ければ地図から経路が消える | 同じ検査を課し，失敗をジョブの失敗として扱う |

1 つ目が重い．次期ダイヤが先に公開されている時期に復旧を試みると，消えるのは現に走っている方である．**壊れた状態を直す操作が，別の壊れ方を生む**．復旧手段は，それ自体をレビューの対象に置く必要がある．

対処を入れたうえで `force` 付きの手動実行を行った．結果は次のとおりである．

表 6. 復旧後の経路形状の件数

| ファイル | 復旧前 | 復旧後 |
|---|---|---|
| `asahikawa_denkikido.json` | 0 | 16879 |
| `dohoku_bus.json` | 0 | 22580 |
| `furano_bus.json` | 0 | 4107 |
| `asahikawa_denkikido_prev.json` | 0 | 0 |

前期間の JSON だけが戻らなかった．材料はキャッシュにある GTFS しかなく，HODA は現行のフィードしか配信しない．過ぎた期間のデータは取り直せない．次のダイヤ改定を待つほかない．

**壊れたものすべてが直せるとは限らない．** 復旧の手を打つ前に，何が戻せて何が戻せないかを分けておく．

---

## 🚧 残る課題

キャッシュの退避そのものは避けられない．週次のワークフローがカレンダー変更を検知した週は，190 MB の取得が発生する．ランナー上では 1 分程度の追加と見込むが，実測は次の該当週まで得られない．

経路形状の欠落は，ジョブサマリの記録と公開直前の検査で捕まえられるようになった．一方，生成された形状が妥当かまでは見ていない．行数が正当に減ったのか劣化したのかは，人が判断する前提のままである．

`asahikawa_denkikido_prev.json` は経路形状を欠いたまま残る．次のダイヤ改定で作り直されるはずだが，実際にそうなるかは当日まで確認できない．今回の実行で形状を含む GTFS がキャッシュへ入ったため，条件は整っている．

[run]: https://github.com/tomio2480/asahikawa-bus-departure/actions/runs/33479590413
[pr119]: https://github.com/tomio2480/asahikawa-bus-departure/pull/119
