# ♿ アクセシビリティ水準の記録

本リポジトリが目指すアクセシビリティの水準を記録する文書．扱う対象は適合レベル，参照したガイドライン，自動検出と手動確認の分担，見直しの周期である．個々の実装がその形を採る理由は `docs/ARCHITECTURE.md` へ，レビューで得た事例は `docs/REVIEW_LESSONS.md` へ置く．本書はその上位にある方針だけを持つ．

---

## 📚 目次

- [🗓 準拠水準](#-準拠水準)
- [🎯 適合レベルを AA とする理由](#-適合レベルを-aa-とする理由)
- [🧭 現状の棚卸し](#-現状の棚卸し)
- [🤖 自動検出の方針](#-自動検出の方針)
- [👀 手動確認の運用](#-手動確認の運用)
- [🔗 既存ドキュメントとの関係](#-既存ドキュメントとの関係)
- [🔁 見直しの手順](#-見直しの手順)

---

## 🗓 準拠水準

- 最終レビュー日: 2026-09-03
- 準拠目標: WCAG 2.2 レベル AA
- 次回見直し推奨: 2027-09-03

表 1. 準拠目標として参照するガイドラインと版

| ガイドライン | 版 | 状態 | 確認日 |
|---|---|---|---|
| WCAG | 2.2 レベル AA | W3C 勧告（2024-12-12 発行）| 2026-09-03 |
| JIS X 8341-3 | 2016 | 現行の規格 | 2026-09-03 |
| デジタル庁ウェブアクセシビリティ導入ガイドブック | 2025 年 10 月 16 日版 | 現行の版 | 2026-09-03 |

いずれも確認日の時点で勧告ないし発行済みの版である．ドラフトの段階にある文書は目標へ含めない．

ウェブアクセシビリティ基盤委員会（WAIC）は 2026-06-08 に，JIS X 8341-3 の改正を準備中と告知した．改正版が公示された時点で本書を見直す．次回見直し推奨日より早めてよい．

---

## 🎯 適合レベルを AA とする理由

- 公開している Web アプリであり，利用者を限定しない．旭川市内のバス利用者が想定読者である．
- 公的機関の調達で求められる水準が AA である．同じ目標へ揃えれば判断がぶれない．
- レベル AAA は，サイト全体への一律適用を WCAG 自身が推奨していない．
- 個人開発であり，専任の検証体制を持たない．満たせない箇所は理由を記録する運用で補う．

---

## 🧭 現状の棚卸し

2026-09-03 時点で確認できた到達点と欠落を記す．網羅的な監査ではなく，方針を決めるための概況である．

表 2. アクセシビリティに関する実装の現状

| 対象 | 状態 |
|---|---|
| ページ言語 | `index.html` へ `lang="ja"` を指定済み |
| トースト | 重大度に応じて `role` と `aria-live` を切り替える |
| バス停検索 | `aria-expanded` を listbox の実在状態へ合わせる |
| 発車案内の表 | 並び替え列へ `aria-sort` を付与．操作は `th` 内のボタンへ委ねる |
| 経路ハイライト | 一覧行へ `tabIndex` と `onKeyDown` を付与する |
| 地図（`MapView.tsx`）| 移動と拡大縮小は Leaflet 既定のキーボード操作が担う．経路ポリラインの選択と，内容の代替表現は未整備 |
| 自動検出 | Biome の `lint/a11y` が `recommended` 経由で有効．`axe-core` が主要 4 部品のテストで走る |

地図の基本操作は Leaflet 1.9.4 が既定で備える．コンテナは `tabindex="0"` を持ち，フォーカスを受け取る．矢印キーによる移動と，`+` / `-` による拡大縮小が使える．ズームボタンにも `role="button"` と `aria-label` が付く．`MapView.tsx` に ARIA 属性を書いていなくても，描画後の地図が配慮を欠くわけではない．

一方，経路ポリラインは SVG の `path` であり，キーボードから選べない．地図が伝える内容の代替表現も用意していない．経路の要点は発車案内の表からも読み取れるが，等価と確認したわけではない．この 2 点を欠落として記録し，対処は Issue #165 で扱う．

---

## 🤖 自動検出の方針

現行の検出手段は Biome の `lint/a11y` である．`biome.json` が `recommended: true` を指定するため，a11y の規則群も有効になる．`ci.yml` の Biome check が Pull Request ごとに走る．

上位の手段として `axe-core` を Vitest のコンポーネントテストで走らせる（Issue #164）．`test/a11y.ts` の `findA11yViolations` が接続済みの要素を検査し，違反を 1 件 1 行で返す．対象は `DepartureBoard`・`StopSearch`・`RouteRegistration`・`Toast` の 4 部品である．部品を足したときは，同じ形で検査を 1 件加える．

表 3. 検討した自動検出ツールと採否

| ツール | 採否 | 理由 |
|---|---|---|
| Biome `lint/a11y` | 採用済み | 静的解析で JSX の誤りを拾う．依存を増やさずに済む |
| `axe-core`（Vitest 経由）| 採用済み | Testing Library と `jsdom` が既にある．`ci.yml` の Test で走る |
| Lighthouse CI | 見送り | ページ単位のスコア計測が主目的．単一画面の SPA では得るものが少ない |
| pa11y | 見送り | ブラウザの実行環境を別途要する．`axe-core` と守備範囲が重なる |

`jsdom` は要素の配置と描画済みの CSS を持たない．そのため `axe-core` を Vitest 経由で動かしても，色のコントラストなど描画に依存する検査は働かない．この範囲は CI の外でブラウザにより補う．手段は「手動確認の運用」に定める．

部品単体のテストでは `region` 規則も外している．部品は `App` の `header` / `main` / `footer` に包まれて初めてランドマークを持つため，単体で描画すると全部品で偽陽性になる．外した規則と理由は `test/a11y.ts` に書き添えてある．

自動修正はいずれのツールでも行わない．検出した指摘はまず妥当性を確かめ，そのうえで修正方針を決める．規則を外す場合は `biome-ignore` へ理由を書き添える．`StopSearch.tsx` の `useSemanticElements` が先例である．WAI-ARIA の combobox パターンが `div` と `role` の組を標準とするため，規則の側を外した．

---

## 👀 手動確認の運用

自動検出が拾える範囲は問題の一部にとどまる．次の確認を併用する．

- UI へ変更を加える Pull Request では，キーボードだけで目的を達成できるか確かめる．
- 観点の一覧は `docs/REVIEW_LESSONS.md` のセルフレビュー用チェックリストに置く．本書へ重ねて書かない．
- スクリーンリーダーによる確認は，トースト・検索の候補一覧・発車案内の表を対象とする．頻度は年 1 回の見直しに合わせる．
- 色のコントラストはブラウザの DevTools（Lighthouse または axe DevTools）で確かめる．ライトとダークの両テーマを対象とする．契機は配色を変える Pull Request と，年 1 回の見直しの 2 つである．

---

## 🔗 既存ドキュメントとの関係

表 4. アクセシビリティに関する記述の置き場所

| 文書 | 扱う内容 |
|---|---|
| 本書 | 準拠目標・検出手段・見直しの周期 |
| `docs/ARCHITECTURE.md` | 個々の実装がその形を採る理由 |
| `docs/REVIEW_LESSONS.md` | レビューで指摘された事例と対処パターン |

本書は方針だけを持ち，実装の詳細を書き写さない．同じ内容を二か所へ置くと，更新のとき片方だけが古くなるためである．

---

## 🔁 見直しの手順

作業の着手時に最終レビュー日を見る．1 年未満なら記録済みの目標に従う．1 年以上が過ぎていれば，次を済ませてから作業へ入る．

1. WCAG・WAIC・デジタル庁のそれぞれで最新版を確認する．ドラフトは対象から外す．
2. 版が変わっていれば，表 1 と準拠目標を書き換える．
3. 表 2 の棚卸しを実コードと突き合わせ，古くなった行を直す．追跡中の Issue（#164・#165）の状態も反映する．
4. 表 3 の自動検出の構成が，現在の CI と一致しているか確かめる．
5. 手動確認（キーボード操作・スクリーンリーダー・色のコントラスト）を実施し，結果を本書へ記録する．
6. 最終レビュー日と次回見直し推奨日を更新する．

外部文書の版と日付だけを更新して済ませない．実装が変わっていれば，その差分が本書へ反映されていることを確認してから日付を進める．

JIS X 8341-3 の改正が公示された場合は，1 年を待たずに見直す．
